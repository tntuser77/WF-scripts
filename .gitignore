# venv + caches
.venv/
__pycache__/
*.pyc
Relics.json
gold_part_price_cache.json
market_catalog.json
market_detail_cache.json
set_cache.json
snapshots/
my_listings_cache.json
my_orders_cache.json
baro_cache.json
baro_flip_cache.json
*.log
# local wiki snapshot (regenerated from live wiki, see web/wiki.py)
input.html
# local sqlite inventory (regenerated from inventory dump + pricing)
relics.db

# local launcher profile (regenerated on launch)
# NOTE: lives under %LOCALAPPDATA%, not the repo, listed here just in case
WFM-Relic-Tools/

# secrets
web/.env
.env
# local WFM session (device id, last presence; token lives in web/.env)
wfm_session.json
